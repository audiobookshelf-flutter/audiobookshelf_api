import 'package:audiobookshelf_api/audiobookshelf_api.dart';

const id = 'root';
const username = 'root';

const json = {
  'id': id,
  'username': username,
};

const sessionUser = SessionUser(
  id: id,
  username: username,
);
